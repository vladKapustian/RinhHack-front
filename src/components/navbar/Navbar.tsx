import Image from "next/image";
import image from "../../../public/Vector.png";

const Navbar = () => {
  return (
    <div>
      <Image src={image} alt="" />
    </div>
  );
};

export default Navbar;
